{
  "name": "ordini-elly-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply ordini-elly-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply ordini-elly-db --local"
  },
  "dependencies": {
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240201.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.111.0"
  }
}
